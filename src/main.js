import { createClient } from "genlayer-js";
import { studionet } from "genlayer-js/chains";
import "./styles.css";
import "./overrides.css";

const RPC_URL = import.meta.env.VITE_RPC_URL || "https://studio.genlayer.com/api";
const CONTRACT = import.meta.env.VITE_CONTRACT_ADDRESS || "0x0000000000000000000000000000000000000000";
const explorer = `https://explorer-studio.genlayer.com/address/${CONTRACT}`;
let readClient = null;
let writeClient = null;
let account = "";

const app = document.querySelector("#app");
app.innerHTML = `
  <header class="masthead"><a class="brand" href="#top"><span class="seal">TS</span><span><b>TraitSeal</b><small>provenance registry</small></span></a><div class="network"><i></i><span id="connection">Read-only inspection</span><button id="wallet">Connect wallet</button></div></header>
  <main id="top">
    <section class="intro"><div><p class="eyebrow">NFT FORENSICS / STUDIONET 61999</p><h1>Provenance you can<br><em>inspect, not assume.</em></h1><p class="lede">TraitSeal locks a creator’s metadata snapshot, then uses GenLayer validators to distinguish an unchanged NFT from a material rewrite.</p><a class="outline" href="#method">Read the methodology <span>↘</span></a></div><aside class="passport"><div class="passport-top"><span>REGISTRY PASSPORT</span><span id="passport-status">UNCONFIGURED</span></div><div class="stamp">TS</div><p class="mono" id="contract-label">${CONTRACT}</p><div class="passport-grid"><span>COLLECTIONS <b id="collections">—</b></span><span>SNAPSHOTS <b id="snapshots">—</b></span><span>CHECKS <b id="checks">—</b></span></div><a class="explorer" href="${explorer}" target="_blank" rel="noreferrer">Open contract in Explorer ↗</a></aside></section>
    <section class="workbench"><div class="section-head"><p class="eyebrow">THE WORKBENCH</p><h2>Seal a collection. Inspect a token.</h2><p>Every action produces an on-chain record. The verifier never overwrites a historical receipt.</p></div><div class="tabs"><button class="tab active" data-tab="register">01 / Register</button><button class="tab" data-tab="inspect">02 / Inspect</button><button class="tab" data-tab="receipt">03 / Receipt</button></div><div id="panel-register" class="panel active"><form id="collection-form"><label>Collection name<input name="name" required placeholder="e.g. Archive Objects" /></label><label>Chain namespace<input name="chain" required placeholder="Ethereum mainnet" /></label><label>NFT contract address<input name="address" required placeholder="0x..." /></label><label>Metadata authority URL<input name="authority" required placeholder="https://... or ipfs://..." /></label><label>Manifest URL<input name="manifest" required placeholder="ar://..." /></label><label>Manifest hash<input name="hash" required placeholder="sha256:..." /></label><button class="primary" type="submit">Register collection <span>→</span></button></form><div class="aside-note"><span class="number">01</span><h3>Lock the authority</h3><p>Registration records who created the collection and which immutable manifest validators should trust.</p><div class="rule"></div><button id="freeze-collection" class="text-button">Freeze a collection ID →</button></div></div><div id="panel-inspect" class="panel"><form id="snapshot-form"><label>Collection ID<input name="collection" type="number" min="0" required placeholder="0" /></label><label>Token ID<input name="token" type="number" min="0" required placeholder="42" /></label><label>Reference metadata URL<input name="metadata" required placeholder="ipfs://..." /></label><label>Reference metadata hash<input name="metadataHash" required placeholder="sha256:..." /></label><label>Image URI or digest<input name="image" required placeholder="ipfs://..." /></label><label>Canonical trait manifest<textarea name="traits" required placeholder='{"background":"ink","edition":"1/1"}'></textarea></label><button class="primary" type="submit">Register token snapshot <span>→</span></button></form><div class="aside-note"><span class="number">02</span><h3>Freeze the token</h3><p>After registering, freeze the snapshot. Then anyone can submit the current metadata URL for an independent inspection.</p><div class="rule"></div><button id="freeze-snapshot" class="text-button">Freeze a snapshot ID →</button></div></div><div id="panel-receipt" class="panel"><form id="verify-form"><label>Frozen snapshot ID<input name="snapshot" type="number" min="0" required placeholder="0" /></label><label>Current metadata URL<input name="current" required placeholder="https://..." /></label><button class="primary" type="submit">Submit inspection <span>→</span></button></form><div class="aside-note"><span class="number">03</span><h3>Read the verdict</h3><p>GenLayer returns bounded facts. TraitSeal derives the final verdict deterministically: VERIFIED, CHANGED or UNVERIFIABLE.</p><div id="last-result" class="result-box">No inspection receipt yet.</div></div></div></section>
    <section id="method" class="method"><div><p class="eyebrow">WHY THIS EXISTS</p><h2>A metadata URL is not a provenance proof.</h2></div><div class="method-copy"><p>Marketplaces often trust mutable JSON, gateways, or creator-hosted pages. TraitSeal preserves the original authority and snapshot before a token is inspected.</p><p class="evidence-note"><strong>Evidence status:</strong> the live matrix includes a verified GitHub commit-pinned match. When a validator cannot fetch a source, the contract deliberately reports <code>UNVERIFIABLE</code> instead of guessing.</p><div class="verdicts"><div><b class="verified">VERIFIED</b><span>All consequential facts match.</span></div><div><b class="changed">CHANGED</b><span>A material trait, image, or identity changed.</span></div><div><b class="unknown">UNVERIFIABLE</b><span>Source unavailable or evidence is ambiguous.</span></div></div></div></section>
    <footer><span>TraitSeal Registry · Intelligent Contract track</span><a href="${explorer}" target="_blank" rel="noreferrer">Contract explorer ↗</a></footer>
  </main><div id="toast" role="status"></div>`;

const header = document.querySelector(".masthead");
const nav = document.createElement("nav");
nav.className = "site-nav";
nav.setAttribute("aria-label", "Primary");
nav.innerHTML = '<a href="#method">How it works</a><a href="#workbench" data-tab="register">Register</a><a href="#workbench" data-tab="inspect">Inspect</a><a href="#workbench" data-tab="receipt">Receipts</a>';
header.insertBefore(nav, header.querySelector(".network"));
document.querySelector(".workbench").id = "workbench";

function toast(message, kind = "") { const el = document.querySelector("#toast"); el.textContent = message; el.className = kind; clearTimeout(window.__toast); window.__toast = setTimeout(() => { el.textContent = ""; el.className = ""; }, 7000); }
function configure() { if (CONTRACT === "0x0000000000000000000000000000000000000000") return; readClient = createClient({ chain: studionet, endpoint: RPC_URL }); document.querySelector("#passport-status").textContent = "LIVE READ"; loadCounts(); }
async function connectWallet() { if (!window.ethereum) return toast("Install MetaMask to sign on Studionet.", "error"); try { const accounts = await window.ethereum.request({ method: "eth_requestAccounts" }); account = accounts[0]; await window.ethereum.request({ method: "wallet_switchEthereumChain", params: [{ chainId: "0xF1EF" }] }); writeClient = createClient({ chain: studionet, endpoint: RPC_URL, account, provider: window.ethereum }); document.querySelector("#connection").textContent = `${account.slice(0, 6)}…${account.slice(-4)} · Studionet`; document.querySelector("#wallet").textContent = "Wallet connected"; toast("Wallet connected. Ready to sign.", "success"); } catch (err) { toast(err.shortMessage || err.message, "error"); } }
function normalizeTxHash(value) { if (typeof value === "string") return value; if (value && typeof value.txId === "string") return value.txId; if (value && typeof value.hash === "string") return value.hash; throw new Error("SDK did not return a transaction hash."); }
async function write(method, args) { if (!writeClient) throw new Error("Connect a funded Studionet wallet first."); const submitted = await writeClient.writeContract({ address: CONTRACT, functionName: method, args, value: BigInt(0) }); const hash = normalizeTxHash(submitted); toast(`Transaction submitted: ${hash.slice(0, 14)}…`); const receipt = await readClient.waitForTransactionReceipt({ hash, status: "FINALIZED", retries: 120, interval: 3000, fullTransaction: true }); if (receipt.statusName && receipt.statusName !== "FINALIZED") throw new Error(`Transaction ${receipt.statusName}`); toast(`Confirmed: ${method}`, "success"); await loadCounts(); return hash; }
async function readVerification(id, txHash) { const raw = String(await readClient.readContract({ address: CONTRACT, functionName: "get_verification", args: [BigInt(id)], stateStatus: "accepted" })); const parts = raw.split("|"); const verdict = parts[4] || "UNVERIFIED"; const facts = parts.slice(5).join("|") || "{}"; document.querySelector("#last-result").innerHTML = `<strong>${verdict}</strong><small>Verification ${id} · ${parts[3] || "—"}</small><code>${facts}</code><a href="https://explorer-studio.genlayer.com/tx/${txHash}" target="_blank" rel="noreferrer">View transaction receipt ↗</a>`; return verdict; }
async function loadCounts() { if (!readClient) return; try { const value = await readClient.readContract({ address: CONTRACT, functionName: "get_counts", args: [] }); const [c, s, v] = String(value).split("|"); document.querySelector("#collections").textContent = c; document.querySelector("#snapshots").textContent = s; document.querySelector("#checks").textContent = v; } catch { document.querySelector("#passport-status").textContent = "READ RETRY"; } }
function formData(form) { return Object.fromEntries(new FormData(form).entries()); }
document.querySelector("#wallet").onclick = connectWallet;
document.querySelectorAll(".tab").forEach((tab) => tab.onclick = () => { document.querySelectorAll(".tab,.panel").forEach((el) => el.classList.remove("active")); tab.classList.add("active"); document.querySelector(`#panel-${tab.dataset.tab}`).classList.add("active"); });
document.querySelectorAll(".site-nav [data-tab]").forEach((link) => link.onclick = () => { const tab = document.querySelector(`.tab[data-tab="${link.dataset.tab}"]`); tab?.click(); });
document.querySelector("#collection-form").onsubmit = async (e) => { e.preventDefault(); const f = formData(e.target); try { const id = await write("register_collection", [f.name, f.chain, f.address, f.authority, f.manifest, f.hash]); toast(`Collection registered. ID ${id || "available after readback"}`, "success"); } catch (err) { toast(err.message, "error"); } };
document.querySelector("#snapshot-form").onsubmit = async (e) => { e.preventDefault(); const f = formData(e.target); try { await write("register_snapshot", [BigInt(f.collection), BigInt(f.token), f.metadata, f.metadataHash, f.image, f.traits]); toast("Token snapshot registered. Freeze it before inspection.", "success"); } catch (err) { toast(err.message, "error"); } };
document.querySelector("#verify-form").onsubmit = async (e) => { e.preventDefault(); const f = formData(e.target); try { const before = String(await readClient.readContract({ address: CONTRACT, functionName: "get_counts", args: [], stateStatus: "accepted" })).split("|"); const verificationId = BigInt(before[2]); const submitHash = await write("submit_verification", [BigInt(f.snapshot), f.current]); const verifyHash = await write("verify_metadata", [verificationId]); await readVerification(verificationId, verifyHash || submitHash); toast("Inspection finalized and receipt loaded.", "success"); } catch (err) { toast(err.message, "error"); } };
document.querySelector("#freeze-collection").onclick = async () => { const id = prompt("Collection ID to freeze:"); if (id !== null) try { await write("freeze_collection", [BigInt(id)]); } catch (err) { toast(err.message, "error"); } };
document.querySelector("#freeze-snapshot").onclick = async () => { const id = prompt("Snapshot ID to freeze:"); if (id !== null) try { await write("freeze_snapshot", [BigInt(id)]); } catch (err) { toast(err.message, "error"); } };
configure();
